PORT=3001

# 9Router (LLM gateway) — jangan pernah commit nilai asli, jangan taruh di frontend
NINEROUTER_URL=https://9router.example/v1/chat/completions
NINEROUTER_API_KEY=
NINEROUTER_MODEL=default

# MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=IsyaRasa

# Opsional — Hermes Agent (Telegram) webhook
HERMES_WEBHOOK_URL=
